package com.netcalculator.netcalculator.service.impl;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
class AccumulatorImplTest {
    @Mock
    private AccumulatorImpl accumulator;


    @Test
    void shouldCalculateActualGrossTest() {
        //        assertEquals(0.0, accumulator.calculateBasicSalary(500, 300));
    }

    @Test
    void shouldCalculateBasicSalaryTest() {
        //        assertEquals(0.0, accumulator.calculateBasicSalary(500,300));
    }
}